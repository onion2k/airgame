
export default class renderer {

    render(bodies) {

        for (let i = 0; i < bodies.length; i += 1) {

            // console.log(bodies[i].position, bodies[i].label);

        }
        
    }

}